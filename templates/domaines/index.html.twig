{% extends 'admin/index.html.twig' %}

{% block title %}Formation index{% endblock %}

{% block content %}
    <div class="col-12">
		<div class="card">
			<div class="card-body">

				<h2 >Liste des Domaines email</h2><br>
                <a class="btn btn-primary waves-effect waves-light" href="{{ path('app_domaines_new') }}" style="width: 20%;">Ajouter un Domaine</a></div>

				<table id="datatable" class="table table-bordered dt-responsive  nowrap w-100">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Ministère</th>
				
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for domaine in domaines %}
            <tr>
                <td>{{ domaine.id }}</td>
                <td>{{ domaine.nom }}</td>
                <td>{{ domaine.categorie }}</td>
			
				
                <td>
					<a class="btn btn-secondary waves-effect waves-light" href="{{ path('app_domaines_show', {'id': domaine.id}) }}">Voir</a>
					<a class="btn btn-warning waves-effect waves-light " href="{{ path('app_domaines_edit', {'id': domaine.id}) }}">Modifier</a>
				</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
		</div>
	</div> <!-- end col -->
 
<style>
div#datatable_wrapper {
    padding: 25px;
}
</style>                    


{% endblock %}
