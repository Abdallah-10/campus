<form method="post" action="{{ path('app_domaines_delete', {'id': domaine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ domaine.id) }}">
    <button class="btn btn-danger waves-effect waves-light">Supprimer</button>
</form>
<style>
    form {
    position: absolute;
    right: 0;
    bottom: 0;
    }
</style>