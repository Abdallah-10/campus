{% extends 'admin/index.html.twig' %}

{% block title %}{% trans %}Partenaire{% endtrans %}{% endblock %}

{% block content %}
    <h1>{% trans %} Partenaire {% endtrans %}</h1>

    <div class="col-md-1">
    </div>
    <div class="col-md-7 mt-4">
    <table class="table">
        <tbody>
            <tr>
                <th>{% trans %} lien {% endtrans %}</th>
                <td>{{ partenaire.link }}</td>
            </tr>
            <tr>
                <th>{% trans %}Cover {% endtrans %}</th>
                <td><img src="{{ asset('assets/uploads/') }}{{ partenaire.cover }}" alt="Client Images" width="80px" ></td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary waves-effect waves-light" href="{{ path('app_partenaire_index') }}">{% trans %} Retour à la liste {% endtrans %}</a>

    <a class="btn btn-warning waves-effect waves-light " href="{{ path('app_partenaire_edit', {'id': partenaire.id}) }}">{% trans %} edit {% endtrans %}</a>

    {{ include('partenaire/_delete_form.html.twig') }}
      </div>
{% endblock %}
