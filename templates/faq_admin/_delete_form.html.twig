<form method="post" action="{{ path('app_faq_admin_delete', {'id': faq.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ faq.id) }}">
    <button class="btn btn-danger waves-effect waves-light">Supprimer</button>
</form>
