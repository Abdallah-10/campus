{% extends 'base.html.twig' %}

{% block title %}Ministere{% endblock %}

{% block body %}
    <h1>Ministere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ministere.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ministere.name }}</td>
            </tr>
            <tr>
                <th>Langue</th>
                <td>{{ ministere.langue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ministere_index') }}">back to list</a>

    <a href="{{ path('app_ministere_edit', {'id': ministere.id}) }}">edit</a>

    {{ include('ministere/_delete_form.html.twig') }}
{% endblock %}
