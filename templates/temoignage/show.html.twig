{% extends 'admin/index.html.twig' %}

{% block title %}Temoignage{% endblock %}

{% block content %}
    <h1>Témoignage</h1>
    <div class="col-md-1">
    </div>
    <div class="col-md-7 mt-4">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ temoignage.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ temoignage.nom }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ temoignage.prenom }}</td>
            </tr>
            <tr>
                <th>Poste</th>
                <td>{{ temoignage.poste }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ temoignage.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary waves-effect waves-light" href="{{ path('app_temoignage_index') }}">Retour à la liste</a>

    <a class="btn btn-warning waves-effect waves-light " href="{{ path('app_temoignage_edit', {'id': temoignage.id}) }}">Modifier</a>

    {{ include('temoignage/_delete_form.html.twig') }}
    </div>
{% endblock %}
