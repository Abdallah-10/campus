<form method="post" action="{{ path('app_temoignage_delete', {'id': temoignage.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ temoignage.id) }}">
    <button class="btn btn-danger waves-effect waves-light supp">Supprimer</button>
</form>
